Web.config
	1) PocketPath пустой => файлы сайта-образца не кэшируются (режим прокси)

url.config
	1) <MessageFrom></MessageFrom>
	   <MessageTo></MessageTo>
	   настройки для отправки форм
	2) <Locked>true</Locked>
	   => запрещает обращаться за файлами к образцу
	3) <Replacement>
		4а) target = UpToClosingTag
		    => считывается всё содержимое тэга, содержащего what (до закрывающего тэга)

/Sheets
	во все тексты кроме css и скриптов внедряется текст из файлов этой папки
/Views/Shared/Controls
	все контролы из этой папки рэндерятся с передачей модели из контроллера и заменяют собой строки в формате @ИмяКонтрола.chtml
